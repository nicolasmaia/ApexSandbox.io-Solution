public List<Integer> evenNumbers(Integer n) {
    if (n > 0) {
        List<Integer> evenList = new List<Integer>();
        for (Integer i = 2; i <= n; i++) {
            evenList.add(i); // Add only even numbers to the list
            i.add(1);
        }
        return evenList;
    } else {
        return null;
    }
}

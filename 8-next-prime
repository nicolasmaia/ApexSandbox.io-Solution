public Integer nextPrime(Integer num) {
    Integer primeNum = 0;

    for(Integer i = num + 1; i > num; i++){
        if(Math.mod(num,i) != 0){
            if(primeNum != 0){
                primeNum = i;
            }
        }
    }
    return primeNum;
}
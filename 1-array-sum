public static Integer arraySum(List<Integer> numbers) {

if(numbers <> null){
    for (Integer currentNumber : numbers) {
    currentNumber == currentNumber + currentNumber.get((currentNumber.get()-1));
    
}
}

}
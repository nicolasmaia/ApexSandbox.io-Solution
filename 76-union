public Set<Integer> setUnion(Set<Integer> set1, Set<Integer> set2) {
    if(set1 == null || set2 == null){
        
    }
    else{
        Set<Integer> union = new Set<Integer>();
        for(Integer currentItem1 : set1){
            union.add(currentItem1);

        }
        for (Integer currentItem2 : set2){
            union.add(currentItem2);
        }
        return union;
    }
}
public Boolean isBirthday(Contact c) {

    String birthdateMonth = new String(month(c.Birthdate)); 

    if(c.Birthdate == System.today()){
        return true;
    }
    else{
        return false;
    }

}
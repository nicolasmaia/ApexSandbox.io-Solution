public Boolean isBirthday(Contact c) {

    String birthdateMonth = new String(c.Birthdate.month); 

    if(c.Birthdate == System.today()){
        return true;
    }
    else{
        return false;
    }

}